<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.stephen.study.modules.test.dao.RoomDao">
    
	<sql id="cmsRoomColumns">
		a.id AS "id",
		a.name AS "name",
		a.create_date AS "createDate"
	</sql>
	
	<select id="get" resultType="Room">
		SELECT 
			<include refid="cmsRoomColumns"/>
		FROM room a
		
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="Room">
		SELECT 
			<include refid="cmsRoomColumns"/>
		FROM room a
		
		<where>
			<if test="name != null and name != ''">
				AND a.name LIKE 
					<if test="dbName == 'oracle'">'%'||#{name}||'%'</if>
					<if test="dbName == 'mssql'">'%'+#{name}+'%'</if>
					<if test="dbName == 'mysql'">CONCAT('%', #{name}, '%')</if>
			</if>
		</where>
	</select>
	
	<select id="findAllList" resultType="Room">
		SELECT 
			<include refid="cmsRoomColumns"/>
		FROM room a
	</select>
	
	<insert id="insert">
		INSERT INTO room(
			id,
			name,
			create_date
		) VALUES (
			#{id},
			#{name},
			#{createDate}
		)
	</insert>
	
	<update id="update">
		UPDATE room SET 	
			name = #{name}
		WHERE id = #{id}
	</update>
	
	<delete id="delete">
		delete from room 
		WHERE id = #{id}
	</delete>
	
</mapper>